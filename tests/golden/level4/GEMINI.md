# Gemini CLI

This project's instructions live in `AGENTS.md`, shared by every coding agent.
If this import is not honoured, set `"context": {"fileName": ["AGENTS.md"]}` in
`.gemini/settings.json` instead.

@AGENTS.md
