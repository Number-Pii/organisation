title = "Fix a reported bug"
smoke = true
prompt = """Front desk reports that back-to-back bookings are being rejected: if one
booking ends at 10:00, nobody can book the same room from 10:00. Please fix it."""
allowed_paths = ["bookings/service.py", "tests/**"]

[expect]
tests = true
validation = true
handover = true
