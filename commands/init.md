---
description: Initialize a Number Pii project (INITIALIZE.md)
---

Initialize a project with the Number Pii toolkit.

Read `${CLAUDE_PLUGIN_ROOT}/INITIALIZE.md` and follow it. It sets out the
decisions to confirm with the user (brief, classification level, team) and
what an initialized project contains. Scaffold with
`python3 ${CLAUDE_PLUGIN_ROOT}/scripts/init_project.py`, with `--output-dir`
pointing at the consuming project, never at the toolkit.

User's arguments: $ARGUMENTS
